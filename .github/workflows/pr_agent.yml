name: PR Agent
on:
  pull_request:
    types: [opened, reopened, ready_for_review]
  issue_comment:
jobs:
  pr_agent_job:
    if: ${{ github.event.sender.type != 'Bot' }}
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
      contents: write
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
      - name: PR Agent action step
        uses: qodo-ai/pr-agent@main
        env:
          OPENAI_KEY: "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"
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OPENAI.ORG: "PerSam"
          PR_REVIEWER.REQUIRE_TESTS_REVIEW: "false" 
          PR_CODE_SUGGESTIONS.NUM_CODE_SUGGESTIONS: 6
